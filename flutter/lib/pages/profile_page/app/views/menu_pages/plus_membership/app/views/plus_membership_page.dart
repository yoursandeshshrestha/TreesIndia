import 'package:flutter/material.dart';

class PlusMembershipPage extends StatelessWidget {
  const PlusMembershipPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Plus Membership'),
      ),
      body: const Center(
        child: Text('Plus Membership Page'),
      ),
    );
  }
}