class NotificationConfig {
  static const String defaultChannel = 'default_channel';
  static const String importantChannel = 'important_channel';

  // Other notification configurations
  static const String defaultTopic = 'general_notifications';
  static const bool enableNotifications = true;
}
